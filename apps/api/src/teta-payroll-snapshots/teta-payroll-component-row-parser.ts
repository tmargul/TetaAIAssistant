/**
 * Stage 3I — component row parser (re-export from section parser).
 */
export {
  parseComponentRows,
  countGenericRecords,
  parseReportSections,
  type ParsedSectionBody,
} from './teta-payroll-report-section-parser';
export type { SectionInventoryResult as SectionParseResult } from './teta-payroll-section-inventory';
